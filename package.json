{
  "name": "mon-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Portfolio statique (HTML, CSS, JS) — déployé sur Vercel",
  "scripts": {
    "dev": "npx --yes serve . -l 3000",
    "start": "npx --yes serve . -l 3000"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.2"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
